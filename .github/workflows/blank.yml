name: Output Information
on: workflow_dispatch 

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Output github context
        env:
          # This maps the context to a shell variable safely
          FULL_CONTEXT: ${{ toJSON(github) }}
        run: echo "$FULL_CONTEXT"
